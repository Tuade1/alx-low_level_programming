#include <string.h>
#include "main.h"

/**
 * _strlen - to count the length of a string
 * @s: the pointer to the string
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
